<%= form_with model: comment, url: post_comments_path(post) do |f| %>

  <%= f.hidden_field :user_id, value: user_id %>
  <%= f.text_field :body, size: '60', placeholder: 'comment here' %> <br>
  <%= f.file_field :attachment %> <br> <br>

  <%= f.submit %>

<% end %>
