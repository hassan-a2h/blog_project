
<%= render partial: 'application/post', object: @post %>
<%= render partial: 'form', locals: { comment: @comment, post: @post, user_id: current_user.id } %>
<%= render partial: 'comment', collection: @comments %>
