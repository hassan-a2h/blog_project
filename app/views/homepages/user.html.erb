
<%= link_to "Create Post", new_post_path %>

<h2> Posts </h2>
<%= render partial: 'application/post', collection: @posts %>

<h2> Comments </h2>
<%= render partial: 'comment', collection: @comments %>
